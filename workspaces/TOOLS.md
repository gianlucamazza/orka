---
tools:
  - name: web_search
    enabled: true
  - name: web_read
    enabled: true
  - name: sandbox
    enabled: true
  - name: echo
    enabled: false
---

## Web Search

Use `web_search` to find current information from the internet. Use `web_read` to fetch and read full web pages.

## Sandbox

Use `sandbox` to execute code snippets safely in a sandboxed environment.
