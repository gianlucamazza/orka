# systemd drop-in override for orka-server filesystem access.
# Installed by scripts/install.sh when allowed_paths includes /home paths.
# ProtectHome=yes creates an inaccessible mount that ReadWritePaths cannot override;
# resetting it here allows ReadWritePaths to bind-mount /home back selectively.

[Service]
ProtectHome=no
ReadWritePaths=/home
